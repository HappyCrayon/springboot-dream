<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.common.mapper.ComnBaseMapper">

    <select id="comnSelect" resultType="java.util.Map">
        ${sql_content}
    </select>

    <insert id="comnInsert">
        ${sql_content}
    </insert>

    <update id="comnUpdate">
        ${sql_content}
    </update>

    <delete id="comnDelete">
        ${sql_content}
    </delete>
</mapper>
